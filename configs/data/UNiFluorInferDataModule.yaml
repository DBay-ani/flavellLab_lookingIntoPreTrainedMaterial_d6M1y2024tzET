_target_: src.data.UNiFluorInferDataModule_datamodule.UNiFluorInferDataModule
data_dir: ${paths.data_dir}
batch_size: 1 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [20,10,5]  # [55_000, 5_000, 10_000]
num_workers: 0
pin_memory: False
